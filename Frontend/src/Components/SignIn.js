import React from 'react';
import ReactDOM from 'react-dom';

 
// rendering app.js which comprices of welcome.js & form.js
ReactDOM.render(
  <React.StrictMode>
      
  </React.StrictMode>,
  document.getElementById('signInRoot')
);
